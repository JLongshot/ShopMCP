{
  "name": "shop-mcp",
  "version": "0.0.0",
  "private": true,
  "description": "A store where the only way to shop is through your AI agent. MCP server + Next.js storefront in a single Vercel deployable.",
  "scripts": {
    "dev": "next dev",
    "build": "next build",
    "start": "next start",
    "lint": "next lint",
    "typecheck": "tsc --noEmit"
  }
}
